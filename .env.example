# Shared party password — everyone uses the same one to log in.
PARTY_PASSWORD=change-me

# Random string used to sign session cookies. Change it to anything long and random.
SESSION_SECRET=change-me-too

# Port the server listens on (optional, default 3001)
PORT=3001
